# Git 仓库迁移说明（2026-07-30）

## 迁移前问题

1. 工作区根目录没有 `.git`。
2. `.git` 错放在 `2026年G题STM32部分/`，导致串口屏、协议、赛题和总体文档不受版本控制。
3. 该子仓库的 `origin` 错指向 `https://github.com/canvson/pcb-skills-repo.git`。
4. 正确目标仓库 `https://github.com/canvson/2026-ddo.git` 已有独立 `main` 历史，不能直接强推覆盖。

## 修复方案

- 将 Git 根目录迁移到整个 G 题工作区。
- 以 `2026-ddo/main` 的 `f2b0f89` 为主线，保留远程已有的屏控波形输出工程。
- 将原 STM32 独立仓库 `0f6719f` 作为第二父历史接入，不引入错误的根级 `F407_ZERO/` 路径。
- 当前 STM32 工程统一位于 `2026年G题STM32部分/F407_ZERO/`。
- 当前串口屏和协议统一位于 `2026年G题串口屏与通信协议/`。
- `origin` 统一指向 `https://github.com/canvson/2026-ddo.git`。
- 本地保留 `legacy/stm32-standalone` 分支，便于查阅迁移前历史。

## 相比上一远程版本的内容变化

- 新增完整的 2026 G 题 STM32、串口屏、协议、赛题和设计文档。
- STM32 固件由旧控制业务调整为 FPGA `FEATURE/STATUS` 被动显示架构。
- 增加 CRC、流式重同步、DMA 回卷、状态超时和 10000 帧混合流回归测试。
- 修复高次谐波固定 121 点显示混叠，改为 121~647 点自适应绘图。
- 多分量缺有效相位时不再伪造波形，而显示 `PHASE REQUIRED`。
- HMI 绘图队列扩大至 32768 字节。
- 所有现行文档同步到实际串口、页面、包类型、相位规则和验收边界。
- 排除 Keil 产物、主机测试二进制、临时目录、自动备份和本地工具状态。

## 验证记录

- STM32 主机测试：全部通过。
- 协议参考自检：通过。
- FPGA 串口仿真自检：通过。
- Keil Target `F407_ZERO`：`0 Error, 0 Warning`。
- 固件占用：Flash 22164 B，RAM 46656 B。

## 恢复保障

迁移前已生成并验证完整 bundle：

```text
.git-stm32-backup-20260730/standalone.bundle
```

该目录仅作本地恢复用途，已被 `.gitignore` 排除，不提交到远程。
