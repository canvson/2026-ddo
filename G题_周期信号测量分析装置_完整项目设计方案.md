# G 题 周期信号测量分析装置完整项目设计方案

更新日期：2026-07-30  
文档原则：明确区分“赛题要求”“设计建议”“当前工作区已实现”和“仍需实测”，不把方案描述当作验收证据。

## 1. 赛题目标

装置对信号源输出的周期信号 `u` 做时域和频域测量，在 6 英寸以上显示屏上显示波形、参数和正频率轴电压频谱。

| 场景 | 被测信号 | 幅度 | 频率范围 | 目标 |
|---|---|---:|---:|---|
| UA | `u=ua` | 100~250 mVpp | 10~200 kHz | 测量并显示 `ua` |
| UB | `u=ub` | 50~250 mVpp | 10~500 kHz | 测量并显示 `ub` |
| UB_J | `u=ub+uJ` | `ub` 50~250 mVpp；`uJ` 200 mVpp | `ub` 10~500 kHz；`uJ>=1MHz` | 抑制干扰后显示 `ub` |

被测信号由基波和 0~2 个谐波组成，频率为 500 Hz 的整数倍，通常只有 1~3 根离散谱线。

核心指标：

- 波形可选择显示 1 个或 3 个完整基波周期。
- 显示整体峰峰值、真有效值和基频。
- Vpp、Urms 绝对误差不超过 5 mV。
- 基频绝对误差不超过 1 kHz。
- 频谱分量幅值绝对误差不超过 5 mV。
- 频率分辨率达到 500 Hz。
- 每项测量与显示应在启动后 2 秒内完成。
- UB_J 场景要避免 1 MHz 以上干扰导致前端/ADC 饱和，并在数字域进一步抑制。

## 2. 当前系统架构

```text
信号源
  -> BNC/50Ω/保护/偏置/增益/模拟低通
  -> 高速 ADC
  -> FPGA：采样、滤波、FFT、谱峰、幅值、基频、相位
  -> FEATURE/STATUS，921600 8N1
  -> STM32F407：协议、状态、波形合成、线谱和 HMI
  -> 串口屏，115200 8N1，page0/page1/page2
```

控制边界：

- `UA/UB/UB_J` 由 FPGA 板上按键选择，FPGA 通过 `FEATURE.mode` 上报。
- 串口屏只切换参数/波形/频谱页和 1/3 周期显示窗口。
- STM32 不下发模式、开始、停止或采集命令。
- STM32 可发送空 payload 的 `PING`，但 FPGA 测量不依赖该命令。

当前工作区包含 STM32 工程、HMI 源资料、协议工具和文档；不包含可审查的 FPGA 工程、模拟前端原理图/PCB 或整机实测记录。因此整机尚不能仅凭本工作区认定验收通过。

## 3. 模拟前端与 ADC

### 3.1 目标链路

```text
BNC
  -> 可确认的 50Ω 端接
  -> 低电容保护与限流
  -> AC 耦合/共模偏置
  -> 低噪声增益
  -> 600~700 kHz 附近模拟低通
  -> 高速 ADC
```

### 3.2 设计要点

- 输入幅度最低 50 mVpp，必须在噪声、量化和增益误差下仍满足 5 mV 误差。
- `ub+uJ` 最坏总输入可能达约 450 mVpp。增益和共模要留出余量，不能先被干扰打满再指望数字滤波恢复。
- 500 kHz 内的增益平坦度、相位延迟可通过标定修正，但不能有不可逆饱和。
- 1 MHz 处需要足够模拟衰减，FPGA FIR 再补充抑制。
- ADC 建议不低于 10 MSPS、12 bit；具体器件、满量程、共模和接口必须以实际硬件为准。

### 3.3 必测项目

- 50 Ω 端接是否导致幅度读数与信号源设置口径一致。
- 50/100/250 mVpp 全范围增益、零点和噪声。
- 10~500 kHz 幅频响应。
- 1 MHz 及以上 200 mVpp 干扰下是否削顶。
- ADC 码流是否有丢样、时钟抖动、跨时钟域错误。

本节是设计要求，不代表当前工作区已有硬件实现。

## 4. FPGA 测量设计

### 4.1 职责

- 产生/接收 ADC 时钟并连续采样。
- 去直流、数字低通、必要的窗函数。
- FFT 和谱峰搜索。
- 识别 1~3 个分量，计算频率、峰值幅度和相对相位。
- 计算整体 Vpp、真有效值和基频。
- 在 UB_J 模式输出抑制干扰后的 `ub` 结果。
- 持续发送 60 字节 `FEATURE`，可选发送 12 字节 `STATUS`。

### 4.2 频率分辨率

FFT 基本关系：

```text
Δf = Fs / N
```

`Fs=8.192MHz、N=16384` 可得到 500 Hz，是可行的设计起点，不是当前工程已证明的 FPGA 参数。最终应结合 ADC 采样率、FPGA 资源、窗函数和插值方法确定。

### 4.3 参数计算

时域：

```text
Vpp = max(x[n]) - min(x[n])
Urms = sqrt(sum(x[n]^2) / N)
```

若 `x[n]` 含 ADC 共模，必须先去直流。Vpp 和 Urms 应按实际前端增益、ADC 满量程及校准系数换算到输入端。

频域：

- 对有限长度数据加合适窗函数，避免非整周期泄漏。
- 按窗函数相干增益和 FFT 标度恢复峰值幅度。
- 在局部峰值附近做插值可提高频率估计，但 500 Hz 分辨率和 1 kHz 误差仍须用标准源验证。
- 分量幅值上报口径为峰值 `amp_peak_uV`，不是 RMS，也不是峰峰值。
- `FEATURE.vpp_uV` 是整体合成波形峰峰值，不是某一个分量的 Vpp。

### 4.4 相位要求

当前 STM32 不接收实测波形点，而是按分量重构：

```text
y(t) = Σ Ai sin(2πfi t + φi)
```

因此：

- 单分量可用 0 度时间参考。
- 多分量必须上报有效相对相位：`flags.bit1=1`，`phase_deg10` 单位 0.1 度。
- FPGA 的相位参考必须在同一采样窗、同一时间原点下定义。
- 多分量缺相位时 STM32 不画假波形，而显示 `PHASE REQUIRED`。

若后续决定传实测波形点，需要新增协议 type，并定义点数、数据类型、缩放、触发点、时间步长、1/3 周期口径和分片机制；当前工程没有该功能。

### 4.5 FPGA 验收证据

- RTL/约束/仿真工程可完整构建。
- ADC 接口与 FFT 时钟时序收敛。
- 内部已知正弦/谐波组合能恢复频率、幅值、相位。
- FEATURE 字节流与协议自测一致。
- 10 kHz~500 kHz、1~3 分量的标准源误差表。
- UB_J 抗干扰前后频谱和波形记录。

## 5. FPGA—STM32 正式协议

### 5.1 外层帧

```text
A5 5A type seq len_l len_h payload... crc_l crc_h
```

- little-endian。
- CRC16/MODBUS，初值 `0xFFFF`，反向多项式 `0xA001`。
- CRC 覆盖 CRC 字段之前的全部帧内容。
- payload 最大 128 字节。

| type | 方向 | 长度 | 作用 |
|---:|---|---:|---|
| `0x10 FEATURE` | FPGA -> STM32 | 60 | 主测量结果 |
| `0x04 STATUS` | FPGA -> STM32 | 12 | 可选状态/统计 |
| `0x85 PING` | STM32 -> FPGA | 0 | 可选链路心跳 |

不存在旧文档中的 `PARAM/WAVE/SPEC/SET_MODE/SET_PERIOD/START/STOP` FPGA 包。

### 5.2 FEATURE

| 偏移 | 字段 | 类型 | 含义 |
|---:|---|---|---|
| 0 | frame_id | u32 | 测量帧号 |
| 4 | mode | u8 | `0=UA,1=UB,2=UB_J` |
| 5 | status | u8 | `0=WAIT,1=VALID,2=HOLD,3=OVER_RANGE,4=ERROR` |
| 6 | component_count | u8 | 1~3 |
| 7 | flags | u8 | bit0 干扰已抑制；bit1 相位有效 |
| 8 | vpp_uV | i32 | 整体 Vpp |
| 12 | urms_uV | i32 | 真有效值 |
| 16 | f1_hz | u32 | 基频 |
| 20 | reserved | u32 | 0 |
| 24/36/48 | component | 12 B | 频率、峰值、相位、保留 |

分量：

```text
u32 freq_hz
i32 amp_peak_uV
i16 phase_deg10
u16 reserved
```

### 5.3 STATUS

```text
u32 frame_id
u8  fpga_state
u8  fpga_error
u16 rx_crc_errors
u16 tx_drops
u16 reserved
```

STATUS 可保持链路在线，但不会让陈旧 FEATURE 重新变成实时。

## 6. STM32F407 实现

### 6.1 实际硬件配置

| 功能 | 配置 |
|---|---|
| HMI | USART1，PA9/PA10，115200 8N1 |
| FPGA | USART2，PA2/PA3，921600 8N1 |
| FPGA RX | DMA1 Stream5，4096 字节循环 DMA |
| HMI TX | USART1 TXE 中断，4096 字节发送环 |
| 绘图队列 | 32768 字节 |
| 周期键 | PE3 低有效 |
| 心跳 LED | PF9 低有效 |

### 6.2 生产代码

唯一生产代码位于：

```text
2026年G题STM32部分/F407_ZERO/Application/Inc
2026年G题STM32部分/F407_ZERO/Application/Src
```

| 模块 | 作用 |
|---|---|
| `fpga_packet.*` | CRC、组包、有界解析、错位重同步 |
| `measure_model.*` | FEATURE/STATUS 校验、单位换算和缓存 |
| `fpga_link.*` | 分发、seq 丢包统计、PING、2/5 秒新鲜度 |
| `bsp_uart.*` | HMI 中断 TX/RX、FPGA 循环 DMA |
| `hmi_protocol.*` | 只解析周期和页面事件 |
| `hmi_screen.*` | 三页文本、波形、频谱、非阻塞队列 |
| `g_app.*` | 裸机轮询、统一状态、页面刷新和心跳 |
| `calibration.*` | 默认比例/偏移及结构 CRC |

串口屏资料包的 `stm32_ref` 只用于无 HAL 协议参考和自测，不参与 Keil Target。

### 6.3 运行状态

| 状态 | 条件 |
|---|---|
| WAIT DATA | 无合法 FEATURE 或 FEATURE=WAIT |
| LIVE | FEATURE 年龄不超过 2 秒且有效 |
| HOLD | FEATURE 超过 2 秒或上报 HOLD |
| COMM ERR | 超过 5 秒无合法 FEATURE/STATUS |
| CRC ERR | 最近 CRC 错误且 FEATURE 已陈旧，尚未有合法帧恢复 |
| OVER RANGE | FEATURE=OVER_RANGE |
| ALGO ERR | FEATURE=ERROR |

页面状态由 `g_app` 统一解析，page0/page1/page2 不会各自得出不同状态。

### 6.4 波形显示修复

原固定 121 点在 `f1=10kHz、fmax=500kHz、3 周期` 时只给 150 个最高频周期约 0.8 点/周期，会严重混叠。当前改为：

```text
intervals = clamp(ceil(4 * periods * fmax / f1), 120, 646)
points = intervals + 1
```

- 最少 120 个线段。
- 最高频每周期至少 4 个线段。
- 最大受 646 像素绘图区限制。
- 合法极限使用 600 个线段、601 点。
- HMI 绘图队列从 16384 增至 32768 字节。
- 主机回归限制最坏命令流不超过 23040 字节，即 115200 8N1 的 2 秒理论串行字节预算。

这是软件层带宽/队列验证；真实串口屏执行 `line` 指令的速度仍须实屏计时。

### 6.5 校准现状

当前 `Calibration_Init()` 每次上电使用：

```text
voltage_gain = 1.0
spectrum_gain = 1.0
freq_offset_hz = 0
```

代码能计算校准结构 CRC，但没有 Flash 读写，也没有已标定系数。达到 5 mV/1 kHz 指标前必须用标准源建立校准表，并决定系数保存在 FPGA、STM32 Flash 或固定版本配置中。

## 7. 串口屏设计

### 7.1 页面

| 页面 | 内容 |
|---|---|
| page0 | 模式、状态、Vpp、Urms、f1、C1~C3 |
| page1 | 1/3 周期波形、右侧参数和状态 |
| page2 | 0~500 kHz 离散线谱、C1~C3 双行值和状态 |

没有设置页、结果页或异常页。异常在当前页状态栏显示。

### 7.2 上行事件

```text
AA CMD LEN DATA CHECK 55
CHECK = (AA + CMD + LEN + sum(DATA)) & 0xFF
```

| CMD | DATA |
|---:|---|
| `0x31 SET_PERIOD` | 1 或 3 |
| `0x32 SET_VIEW` | 0 参数、1 波形、2 频谱 |

### 7.3 下行绘图

STM32 使用 `page/fill/xstr/line`，命令尾为 `FF FF FF`。字体：

- ID1：普通动态字段。
- ID2：page2 频率/幅值的 16 像素双行文字。
- ID3：预留，不参与主数据。

资料包已有 `.HMI` 模板、ID1/ID2/ID3 字体和三页底图，但没有经本次检查确认的可下载 `.tft` 与真实屏幕录像。因此它是可联调源资料，不是实屏验收证据。

## 8. 验证路线

### 8.1 已有自动验证

STM32 主机测试覆盖：

- CRC 已知向量、组包、噪声/错头/坏长度/坏 CRC 恢复。
- FEATURE/STATUS 字段、非法模式/数量/长度。
- seq 丢包、STATUS-only、PING-only 上行。
- HMI 五个合法事件和非法帧。
- 1999/2001/4999/5001 ms 状态边界与恢复。
- DMA 回卷/覆盖窗口。
- 10000 个混合 FEATURE 帧、周期性 CRC 故障和错位恢复。
- HMI 背压队列、page2 ID2 双行。
- 多分量缺相位不画波形。
- `10kHz/500kHz/3周期` 600 段极限绘图和 2 秒字节预算。

协议工具：

```text
python -B host_test/fpga_serial_sim.py --self-test
```

Keil：

```text
MDK-ARM/F407_ZERO.uvprojx
Target: F407_ZERO
要求: 0 Error, 0 Warning
```

### 8.2 必须补齐的实测

1. HMI 编辑器编译、下载和三页真实触摸测试。
2. 115200 下极限 601 点波形的实际完成时间和指令丢失检查。
3. FPGA 连续 FEATURE、STATUS-only、断流、CRC 故障实串口测试。
4. 多分量相位与示波器/离线采样波形对比。
5. UA/UB 全频段 Vpp、Urms、f1、谱峰幅值误差表。
6. UB_J 加干扰前后误差、前端是否饱和、抑制度记录。
7. 连续运行、掉电重启、电源纹波和温漂测试。

## 9. 赛题覆盖与工程状态

| 要求 | 当前工作区状态 | 验收判断 |
|---|---|---|
| 1/3 周期显示 | 固件和 HMI 事件已实现 | 需实屏 |
| 高次谐波波形 | 自适应 121~647 点已实现并主机测试 | 需实屏/实测波形 |
| 波形真实性 | 多分量强制有效相位 | 需 FPGA 提供相位并对比 |
| 参数显示 | 已实现 | 需校准 |
| 0~500 kHz 线谱 | 已实现 | 需实屏和幅值校准 |
| 2 秒状态目标 | 最坏串口字节预算通过 | 需整机计时 |
| 5 mV / 1 kHz | 无校准/标准源记录 | 未验收 |
| 500 Hz 分辨率 | FPGA 设计目标 | 工作区无 FPGA 证据 |
| UB_J 抗干扰 | 仅协议/显示支持 | 工作区无 AFE/FPGA/实测证据 |
| STM32 构建与回归 | 可自动验证 | 每版复验 |
| HMI 工程 | 有源文件和字体 | 无 `.tft`/实屏证据 |

## 10. 团队分工

### 模拟前端/ADC

- 原理图、PCB、BOM、端接/增益/滤波说明。
- 输出满量程、幅频、噪声和抗干扰报告。
- 给 FPGA 明确 ADC 编码、时钟、共模、增益和校准接口。

### FPGA

- 可构建 RTL、约束、仿真和资源/时序报告。
- 统一 FEATURE 相位参考并保证多分量 `phase_valid=1`。
- 输出参数/频谱误差和 UB_J 抗干扰记录。
- 与 STM32 仿真器和实串口逐字段对齐。

### STM32/串口屏

- 保持 `Application` 为唯一生产代码。
- 每版运行主机测试、协议自测、Keil 0 警告构建。
- 完成 HMI 编译下载、ID2 字体和极限波形实屏验收。
- 完成校准系数、保存策略和整机验收表。

## 11. 最终交付清单

- [ ] 模拟前端/ADC 原理图、PCB、BOM和测试点说明。
- [ ] FPGA RTL、约束、仿真、构建和时序报告。
- [ ] STM32 Keil 工程、固件产物、构建日志和主机测试结果。
- [ ] HMI 源工程、字体、可下载产物和实屏证据。
- [ ] 最终协议版本，FPGA/STM32/HMI 三方一致。
- [ ] UA/UB/UB_J 标准源全范围测试表。
- [ ] Vpp、Urms、f1、谱峰幅值校准数据。
- [ ] 2 秒完成时间、长时间稳定性、断线/坏帧恢复记录。
- [ ] 多分量有效相位与波形真实性对比记录。
- [ ] 赛题逐条覆盖表，所有“已通过”均有可复查证据。

## 12. 当前结论

STM32 软件链路已具备较完整的协议、防错、状态和显示实现，高次谐波固定点数混叠及缺相位伪波形问题已在代码和自动测试中修复。

但“STM32+串口屏达到验收级”仍需 HMI 实屏、真实 FPGA 串口、极限波形完成时间、校准精度和整机稳定性证据；“整机达到赛题要求”还必须补齐 FPGA 与模拟前端工程及测量报告。
